# ODE Adapter — Java

Java reference implementation. Contract lives in `../spec/`. Build with Maven
(`mvn -q test`). Start from `src/main/java/net/oralhealthalliance/ode/ports/`.
See `STATUS.md` for current state and `../PROGRAM-PLAN.md` for the roadmap.
