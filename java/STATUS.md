# Java implementation — STATUS

**State: starter skeleton — community-owned. PRs welcome.**

Implements: the three ports (interfaces) from `spec/contract/ports.md` and the
envelope records. No transactions implemented yet.

Roadmap (see `/PROGRAM-PLAN.md` Phase 3): bring to parity with Python against
`spec/` and the conformance checklist. Suggested first task: PCC-55 inbound
(C-CDA Referral Note → FHIR Bundle) with the `generic-r4` FhirBackend.
