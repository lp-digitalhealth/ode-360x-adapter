# Container for the Python reference adapter + its HTTP faces.
FROM python:3.12-slim
WORKDIR /app
COPY python/ /app/
RUN pip install --no-cache-dir -e ".[server,fhir]"
ENV ODE_ADAPTER_DRY_RUN=false
EXPOSE 8000
CMD ["uvicorn", "ode_adapter.app:app", "--host", "0.0.0.0", "--port", "8000"]
