## Summary

<!-- What does this PR change, and why? -->

## Contract (`spec/`) impact

<!-- Does this change behavior? If so, confirm spec/ was updated first.
     The golden rule: implement against spec/, not another language's code. -->

- [ ] No behavior change (docs / refactor / tooling only), **or**
- [ ] `spec/` updated to reflect the behavior change

## Implementations touched

- [ ] `python/`
- [ ] `dotnet/`
- [ ] `java/`
- [ ] `node/` (demo UI)
- [ ] `spec/` / docs only

## Verification

<!-- How was this validated? e.g. Python tests, .NET test runner (N/N), demo run,
     conformance checklist items. -->

## Checklist

- [ ] Validated against the `spec/` fixtures / conformance checklist where applicable
- [ ] No build artifacts committed (`bin/`, `obj/`, `*.egg-info/` are ignored)
- [ ] Ready for at least one committer review (see `CHANGE_MANAGEMENT.md`)
